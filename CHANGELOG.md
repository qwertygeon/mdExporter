# Changelog

이 프로젝트의 주요 변경 사항을 기록한다. 형식은 [Keep a Changelog](https://keepachangelog.com/), 버전은 [Semantic Versioning](https://semver.org/) 을 따른다.

## [Unreleased]

### Added

- mermaid 다이어그램을 그림으로 담는다 — ` ```mermaid ` 로 적은 순서도·시퀀스 다이어그램 등을 변환 시점에 그려 HTML·PDF 에 담는다. 산출물에는 그림만 들어가고 스크립트가 붙지 않아, 볼 때 네트워크가 필요 없고 HTML 과 PDF 가 같은 그림을 공유한다. 그림을 그리는 데 PDF 와 같은 Chromium 이 쓰이며, 브라우저가 없거나 문법 오류가 있으면 경고와 함께 그 다이어그램만 코드블록으로 남고 변환은 계속된다. 원시 HTML `<div class="mermaid">` 로 적은 것은 그리지 않고 경고한다.
- `--no-mermaid` — 다이어그램을 그리지 않고 코드블록으로 남긴다(브라우저를 띄우지 않는다).
- 테마에서 다이어그램 색·크기를 정할 수 있다 — 테마 CSS `:root` 에 `--mermaid-fontSize` 같은 mermaid 테마 변수, `--mermaid-config-flowchart-nodeSpacing` 같은 mermaid 설정을 선언하면 다이어그램에 적용된다. 선언하지 않으면 mermaid 내장 테마가 쓰이며, 색을 바꿀 때는 `--mermaid-config-theme: base` 를 함께 선언한다. 다이어그램마다 고유 크기가 `--mdx-diagram-w`·`--mdx-diagram-h` 로 전달되어 테마가 자체 크기 정책을 쓸 수 있다.
- 다이어그램 대체 텍스트 — 다이어그램 안에 `accTitle`·`accDescr` 를 적으면 산출물에 실려 화면 낭독기가 읽는다.
- 기본 테마가 다이어그램 크기를 인쇄 본문에 맞춘다 — 글자 13px·흐름도 노드 간격 축소. mermaid 기본 16px 은 본문(10.5pt)보다 커서 도형이 부풀고, 10단계 규모 흐름도가 지면 높이의 88% 를 차지해 제목과 다른 페이지로 밀려났다.
- 로컬 이미지를 산출물에 담는다 — 문서가 참조하는 이미지를 HTML·PDF 안에 인라인하므로 산출물만 옮겨도, PDF 로 뽑아도 이미지가 보인다. 경로는 입력 파일 위치 기준으로 해석하고(하위 폴더·상위 참조·절대경로 모두), 원격 주소와 이미 인라인된 이미지는 손대지 않는다. 담지 못한 참조(경로 없음·읽기 실패·이미지가 아닌 확장자·빈 파일·원시 HTML `<img>` 태그)는 원인과 함께 경고로 알린다. 대소문자만 다르게 참조한 경우는 담기지만, 대소문자를 구분하는 환경에서 깨질 것을 미리 경고한다.
- `--no-embed-images` — 이미지를 담지 않고 원본 경로를 유지한다(산출물이 작아지는 대신 PDF 에서 이미지가 보이지 않는다).

### Changed

- mermaid 다이어그램이 있는 문서는 옵션을 주지 않아도 산출물이 커진다 — 다이어그램 하나당 대략 15~25KB 다. 종전 크기를 유지하려면 `--no-mermaid` 를 쓴다.
- 이미지가 있는 문서는 옵션을 주지 않아도 산출물이 커진다 — 담은 이미지 용량의 약 4/3 만큼이며, 같은 이미지를 여러 번 참조하면 참조 수만큼 실린다. 종전 크기를 유지하려면 `--no-embed-images` 를 쓴다.

### Fixed

- PDF 에서 상대경로 이미지가 조용히 사라지던 문제 — 변환기가 PDF 를 만들 때 문서에 기준 경로가 없어 이미지가 전부 빈칸이 되는데도 성공으로 보고했다. `--out-dir` 로 다른 폴더에 출력한 HTML 도 같은 이유로 이미지가 깨졌다.
- 넓은 이미지가 지면 폭을 넘어 잘리던 문제 — 기본 테마에 이미지 최대 너비 규칙을 추가했다.

## [0.3.0] - 2026-08-03

### Added

- npm 패키지 배포 — `npm install -g mdexporter` 로 설치하면 `mdexport` 명령을 바로 쓸 수 있다. 소스를 클론해 빌드할 필요가 없다.
- 다중 문서 일괄 변환 — 여러 `.md` 파일이나 디렉터리를 한 번에 변환(`mdexport <입력...>`). glob 은 shell 확장, 디렉터리는 그 안의 `.md`/`.MD`(대소문자 무관, 기본 비재귀).
- 일괄 PDF 변환 시 Chromium 인스턴스 재사용 — 문서마다 재실행하지 않고 한 번 띄워 처리 후 정리.
- 입력 오류를 조용히 넘기지 않는다 — 변환 대상 `.md` 가 하나도 없거나 존재하지 않는 경로를 주면 안내 메시지와 함께 오류로 멈춘다.
- 여러 입력이 같은 출력 경로로 향하면 변환을 시작하기 전에 차단한다 — 앞선 산출물이 조용히 덮어써지지 않는다. 대소문자를 구분하지 않는 파일시스템(macOS·Windows)에서 이름이 대소문자만 다른 입력(`Guide.md`·`guide.md`)이 같은 파일이 되는 경우까지 감지하며, 대상 파일시스템의 실제 동작을 확인하고 판단하므로 대소문자를 구분하는 환경의 정상적인 사용은 막지 않는다.
- 하위 디렉터리 재귀 탐색 (`-r`, `--recursive`) — 디렉터리 입력의 하위 트리 전체를 수집한다. `--out-dir` 과 함께 쓰면 입력 구조를 출력 디렉터리 아래에 그대로 재구성한다(평탄화 아님). `.` 으로 시작하는 디렉터리와 `node_modules` 는 건너뛰고, 디렉터리 심링크는 따라가지 않아 순환에서 멈추지 않는다.
- 기본 테마의 오프라인 한글 폰트 — Pretendard(상용 한글 2350자 + 영문)를 CSS 에 직접 담아, 네트워크가 없거나 CDN 이 차단된 환경에서도 온라인과 동일하게 렌더된다. 폰트 라이선스(SIL OFL 1.1)를 함께 배포한다.
- 대안 테마 두 가지 — `themes/default-full.css`(현대 한글 11172자 전체 임베딩, 완전 커버) · `themes/default-cdn.css`(임베딩 없이 CDN 원격 로드, 크기 최소). `--theme` 으로 선택한다.
- 폰트 재현 빌드 스크립트(`scripts/build-fonts.py`) — 서브셋 생성부터 테마 임베딩까지 재실행해도 같은 결과를 낸다. 빌드타임 전용이라 런타임 의존성은 늘지 않는다.

### Changed

- 기본 테마가 더 이상 네트워크를 타지 않는다 — 폰트 임베딩 때문에 산출물 크기가 종전 대비 약 420KB 늘어난다. 크기가 우선이면 `themes/default-cdn.css` 로 종전 방식을 쓸 수 있다.
- `.gitignore` 의 `*.html`/`*.pdf` 전역 무시를 루트 스크래치(`/*.html`·`/*.pdf`) 한정으로 좁힘 — 저장소 자산으로 두려는 html/pdf 는 다른 경로에서 추적 가능.
- 입력이 여러 개면 `--title` 은 무시되고 각 파일명이 제목으로 쓰인다(단일 입력은 종전대로).

## [0.2.0] - 2026-07-24

### Added

- 자동 목차 생성 (`--toc`, 깊이 `--toc-depth`) — 헤딩 앵커 + 링크.
- 표지 페이지 (`--cover`, 날짜 `--date`).
- PDF 헤더/푸터 (`--header` 문서 제목, `--footer` 페이지 번호).

모든 레이아웃 기능은 opt-in 이며, 지정하지 않으면 산출물은 v0.1.0 과 동일하다(기본 동작 불변).

## [0.1.0] - 2026-07-24

### Added

- Markdown → HTML/PDF 변환 CLI (`mdexport`).
- HTML/PDF 동일 디자인 산출 (브라우저 인쇄 기반, 단일 CSS 테마).
- 한글 시인성 우선 내장 기본 테마.
- 교체 가능한 테마 주입 옵션 (`--theme`).
- CLI 옵션: 출력 포맷(`--format`), 제목(`--title`), 출력 디렉터리(`--out-dir`).
- 원본 문구를 보존하는 내용 불변 변환.
