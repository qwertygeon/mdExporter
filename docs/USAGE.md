# 사용자 매뉴얼

mdExporter 로 Markdown 문서를 HTML·PDF 로 변환하는 방법을 설명한다.

## 목차

- [기본 사용법](#기본-사용법)
- [일괄 변환 (다중 입력)](#일괄-변환-다중-입력)
  - [하위 디렉터리까지 재귀 --recursive](#하위-디렉터리까지-재귀---recursive)
- [옵션](#옵션)
  - [--format](#--format)
  - [--theme](#--theme)
  - [--title](#--title)
  - [--out-dir](#--out-dir)
- [문서 레이아웃 (opt-in)](#문서-레이아웃-opt-in)
  - [목차 --toc](#목차---toc)
  - [표지 --cover](#표지---cover)
  - [헤더/푸터 --header / --footer](#헤더푸터---header----footer)
- [출력 규칙](#출력-규칙)
- [PDF 페이지 설정](#pdf-페이지-설정)
- [내용 불변 원칙](#내용-불변-원칙)
- [트러블슈팅](#트러블슈팅)

## 기본 사용법

```
mdexport <input.md> [옵션]
```

`<input.md>` 는 변환할 Markdown 파일 경로다. 소스에서 빌드해 쓰는 경우 `mdexport` 대신 `node dist/cli.js` 로 실행한다.

옵션 없이 실행하면 입력 파일과 같은 폴더에 HTML 과 PDF 를 모두 생성한다.

```bash
mdexport 문서.md
# → 문서.html, 문서.pdf 생성
```

## 일괄 변환 (다중 입력)

`<입력...>` 에 여러 파일이나 디렉터리를 줄 수 있다. 각 문서는 개별적으로 변환되어 각자의 `.html`/`.pdf` 를 만든다.

- **여러 파일**: `*.md` 같은 glob 은 shell 이 파일 목록으로 확장해 넘긴다.
- **디렉터리**: 디렉터리를 주면 그 안의 `.md` 파일(하위 폴더는 제외 — 비재귀)을 모두 변환한다. 대소문자 무관하게 `.md`/`.MD` 를 모두 인식한다.
- **PDF 성능**: 여러 문서를 PDF 로 변환할 때 Chromium 을 매 문서마다 재실행하지 않고 **한 번 띄워 재사용**한 뒤 마지막에 정리한다.
- **`--title`**: 확장 후 대상 파일이 여러 개면 문서마다 같은 제목을 강제하지 않도록 `--title` 은 무시되고 각 파일명이 제목이 된다(경고를 출력한다). 단일 디렉터리 입력이라도 그 안에 `.md` 가 여러 개면 무시된다.
- **매칭 0건**: 입력(디렉터리)에서 변환할 `.md` 를 하나도 찾지 못하면 조용히 종료하지 않고 오류로 알린다(0건을 성공으로 오인하지 않도록).

```bash
# 여러 파일 (shell glob)
mdexport docs/*.md -f pdf -o out/

# 디렉터리 안의 모든 .md
mdexport ./my-docs -f html -o out/
```

### 하위 디렉터리까지 재귀 --recursive

`-r, --recursive` — 디렉터리 입력을 하위 트리까지 전부 순회한다. 지정하지 않으면 위와 같이 최상위 `.md` 만 변환한다(기본 동작 불변).

- **스킵 규칙**: `.`(dot)으로 시작하는 디렉터리(`.git`, `.claude` 등)와 `node_modules` 는 순회에서 제외한다.
- **순환 방지**: 디렉터리 심링크는 순회 대상에서 제외되며, 심링크로 순환 구조가 생겨도 무한루프 없이 종료한다.
- **출력 미러링**: `--out-dir` 를 함께 지정하면, 산출물은 스캔한 각 디렉터리 기준 상대경로를 그대로 반영해 `--out-dir` 아래에 재구성된다(평탄화 아님) — 예: `my-docs/sub/x.md` → `out/sub/x.html`. `--out-dir` 를 지정하지 않으면 각 산출물은 원본 파일 옆에 그대로 생성된다.
- 파일 인자(디렉터리가 아닌)에는 `-r` 이 영향을 주지 않는다.

```bash
# 하위 디렉터리까지 모두 변환, 폴더 구조를 out/ 아래에 그대로 재구성
mdexport ./my-docs -r -o out/
```

## 옵션

### --format

`-f, --format <format>` — 생성할 포맷을 지정한다.

| 값 | 결과 |
|---|---|
| `both` (기본) | HTML 과 PDF 모두 |
| `html` | HTML 만 (브라우저 불필요) |
| `pdf` | PDF 만 |

```bash
mdexport 문서.md -f html
```

### --theme

`-t, --theme <path>` — 사용할 테마 CSS 파일 경로. 지정하지 않으면 내장 기본 테마를 쓴다.

```bash
mdexport 문서.md -t my-theme.css
```

테마 작성·커스터마이즈는 [테마 가이드](THEMING.md) 참조.

### --title

`--title <title>` — HTML 산출물의 `<title>` 값. 지정하지 않으면 입력 파일명(확장자 제외)을 쓴다.

```bash
mdexport report.md --title "월간 보고서"
```

### --out-dir

`-o, --out-dir <dir>` — 산출물을 저장할 디렉터리. 지정하지 않으면 입력 파일과 같은 폴더에 저장한다. 폴더가 없으면 생성된다.

```bash
mdexport report.md -o build/
```

## 문서 레이아웃 (opt-in)

아래 옵션은 모두 선택이며, 지정하지 않으면 산출물은 이전과 동일하다(기본 동작 불변). 목차·표지·헤더/푸터는 **생성 요소**로만 추가되고 원문 본문 내용은 그대로 보존된다.

### 목차 --toc

`--toc` — 문서 헤딩에서 목차를 만들어 (표지 뒤) 본문 앞에 넣는다. 헤딩에 앵커가 부여되고 목차 항목이 해당 헤딩으로 링크된다.

`--toc-depth <n>` — 목차에 포함할 헤딩 깊이. 기본 3(h2~h3). h1 은 문서 제목으로 간주해 목차에서 제외한다.

```bash
mdexport 문서.md --toc                 # h2~h3 목차
mdexport 문서.md --toc --toc-depth 4    # h2~h4
```

### 표지 --cover

`--cover` — 첫 페이지에 표지(제목)를 넣고 뒤에서 페이지를 나눈다. 제목은 `--title`(없으면 파일명)을 쓴다.

`--date <date>` — 표지에 표시할 날짜. **지정할 때만** 표시된다.

```bash
mdexport 문서.md --cover --title "월간 보고서" --date 2026-07-24
```

### 헤더/푸터 --header / --footer

**PDF 전용** — HTML 에는 페이지 개념이 없어 적용되지 않는다.

- `--header` — 페이지 상단에 문서 제목.
- `--footer` — 페이지 하단에 페이지 번호(`현재 / 전체`).

활성 시 상·하 여백을 자동으로 넉넉히 잡아 헤더/푸터가 본문과 겹치거나 잘리지 않게 한다.

```bash
mdexport 문서.md -f pdf --header --footer --title "월간 보고서"
```

## 출력 규칙

- 산출물 파일명은 입력 파일명을 따른다: `report.md` → `report.html`, `report.pdf`.
- 저장 위치는 `--out-dir` 지정 시 그 폴더, 아니면 입력 파일과 같은 폴더.
- 성공 시 생성된 파일 경로가 `✓ <경로>` 형태로 출력된다.

> `--out-dir` 를 지정하지 않으면 산출물이 **입력 파일과 같은 폴더에 생성**된다. 저장소의 `.gitignore` 는 `*.html`/`*.pdf` 를 레포 루트에 한정해 무시하므로, 하위 폴더의 입력을 변환하면 그 산출물은 자동으로 무시되지 않는다 — 커밋을 원치 않으면 `--out-dir` 로 무시 경로를 지정하거나 산출물을 정리한다.

## PDF 페이지 설정

현재 PDF 는 아래 고정 설정으로 생성된다:

- 용지: A4
- 여백: 상하 18mm, 좌우 15mm
- 배경 인쇄: 켜짐 (표 헤더·콜아웃·코드 박스 배경이 인쇄에 포함됨)

페이지 나눔은 테마의 인쇄 규칙을 따른다 — 표·코드 블록·콜아웃은 페이지 경계에서 잘리지 않게 처리되며, 한 페이지보다 큰 요소는 예외적으로 나뉜다.

## 내용 불변 원칙

변환은 원본 Markdown 에 **스타일만 입힌다.** 문구·구조를 바꾸지 않으며, 자동 인용부호 치환·자동 링크화 같은 원문 변형 처리를 하지 않는다. 산출물의 텍스트 내용은 원본과 일치한다.

## 트러블슈팅

| 증상 | 원인 · 해결 |
|---|---|
| PDF 생성 시 브라우저 관련 오류 | Chromium 미설치 — `npx playwright install chromium` 로 1회 설치 |
| 한글이 기본 고딕으로 나옴 | 네트워크 차단으로 웹폰트(Pretendard) 로드 실패 → 시스템 폰트로 폴백된 것. 온라인에서 재시도 |
| 표·코드 박스 배경이 PDF 에서 안 보임 | 기본 설정은 배경 인쇄 켜짐 — 커스텀 테마에서 배경을 제거하지 않았는지 확인 |
| 스타일이 안 먹음 | `--theme` 경로가 실행 위치 기준으로 유효한지 확인 |
| 표·코드가 페이지 경계에서 잘림 | 요소가 한 페이지보다 크면 불가피 — 내용을 나누거나 테마 폰트 크기를 조정 |
